--[[ Custom plugin config loader. ]]--

-- NOTE: Those 'extra' configurations won't most likely be changed again.
-- You are free to edit/re-enable them if you wish to do so.

local extra = ... .. "."

-- NOTE: in order to avoid loading unnecessary files, they all have been commented out.

-- Load File Tree Settings.
-- require(extra .. "_nvimtree")

-- Load old git signs.
-- require(extra .. "_oldgitsigns")

-- Load old completion behavior.
-- require(extra .. "_tabautocomp")

-- Load some bufferline config I made.
-- require(extra .. "_bufferline")

-- Not quite what I was looking for. But i'm still leaving it there now.
-- require(extra .. "_wintabs")
