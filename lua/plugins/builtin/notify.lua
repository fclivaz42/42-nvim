return {
	-- Adds some nice notifications!
	-- Useful so you don't get interrupted by the bottom line screaming at you.
	'rcarriga/nvim-notify',
	opts = {
		fps = 60,
		render = 'compact',
		timeout = 100,
	},
	lazy = true
}
