-- [[
-- I have decided to 'Eject' image.nvim out of the built-in and moved it here instead since it was supposed to be a part of 42-Nvim.
-- Since it was sometimes causing issues on cluster computers, I yanked it out as well as its options to avoid causing problems.
-- Turn on at your own leisure.
-- ]] --

return {
-- 	"3rd/image.nvim", -- Optional image support in preview window: See `# Preview Mode` for more information
-- 	build = false,
-- 	lazy = true,
-- 	opts = {
-- 		processor = "magick_cli"
-- 	}
}
